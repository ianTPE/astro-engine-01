{
	"name": "astro-engine-01",
 "type": "module",
 "version": "0.0.1",
  "scripts": {
    "predev": "bun install --frozen-lockfile",
    "dev": "bunx astro dev",
    "prebuild": "bun install --frozen-lockfile",
    "build": "bunx astro build",
    "prepreview": "bun install --frozen-lockfile",
    "preview": "bunx astro build && bunx wrangler pages dev",
    "astro": "bunx astro",
    "predeploy": "bun install --frozen-lockfile",
    "deploy": "bunx astro build && bunx wrangler pages deploy",
    "cf-typegen": "bunx wrangler types"
  },
	"dependencies": {
		"@astrojs/cloudflare": "^12.6.11",
		"@astrojs/mdx": "^4.3.12",
		"@astrojs/rss": "^4.0.14",
		"@astrojs/sitemap": "^3.6.0",
		"astro": "^5.16.0",
		"sharp": "^0.34.3"
	},
 "devDependencies": {
    "@types/node": "^24.10.1",
    "wrangler": "^4.49.1"
  },
  "packageManager": "bun@1.3.2"
}
